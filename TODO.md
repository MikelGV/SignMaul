## TO-DO List
1. set up infrastructure
1.1. set up containarization
1.2. set up development environment
1.3. set up a basic infrastructure example, and the safe to upload infra files
2. set up backend
2.1. set up a the server logic
2.2. set up the config and the utility functions (e.g. my custom encrypt decrypt functions)
2.3. set up the database logic
2.4. set up cors and other needed middlewares
2.5. set up user crud (both handlers and database queries needed)
2.6. set up auth handlers
2.7. set up the signature editor backend logic (you should be able to save 
    signature templates so that you don't need to redo them every time), it 
    should convert everything into html 
2.8. set up the logic for handling when the user gets and update on the recorded pixel
2.9. set up the analytics logic
2.10. set up the gmail, outlook, and other email webhooks so that the user can send the mails
3. set up frontend
3.1. set up templ
3.2. set up htmx
3.3. set up tailwind
3.4. set up sortable 
3.4. set up the footer and the topbar as independent components that can be refurbished
3.5. set up the landing page
3.6. set up the sign up/in pages
3.7. set up the dashboard(here u should be able to see a preview of your stats) 
    and lock it for when the user is logged in, also make the landing page unreachable when log in
3.8. set up the signature editor ui
